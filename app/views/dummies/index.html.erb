<h1>Listing dummies</h1>

<table>
  <tr>
    <th>Dum1</th>
    <th>Dum2</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @dummies.each do |dummy| %>
  <tr>
    <td><%= dummy.dum1 %></td>
    <td><%= dummy.dum2 %></td>
    <td><%= link_to 'Show', dummy %></td>
    <td><%= link_to 'Edit', edit_dummy_path(dummy) %></td>
    <td><%= link_to 'Destroy', dummy, :confirm => 'Are you sure? This deletes the video file as well.', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dummy', new_dummy_path %>
